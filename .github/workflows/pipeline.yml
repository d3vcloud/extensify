name: Publish extension

on:
  push:
    branches:
    - master
  release:
    types:
    - created

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-20.04]
    runs-on: ${{ matrix.os }}
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Listing files
      run: ls -la && git --version
    - name: Install Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 16.x
    - run: cd extension && npm install
    - name: Publish
      if: matrix.os == 'ubuntu-20.04'
      run: cd extension && npm run deploy git --git-dir="/Users/nxdev/Cp/extensions/extensify/.git" -m "Testing"
    env:
      VSCE_PAT: ${{ secrets.VSCE_PAT }}